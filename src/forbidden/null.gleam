import forbidden/dynamic

/// Unleash the billion dollar mistake into your app
pub fn new() -> a {
  dynamic.coerce(Nil)
}

/// Checks if value is secretly Nil
pub fn is(nullable: a) -> Bool {
  dynamic.coerce(nullable) == Nil
}
